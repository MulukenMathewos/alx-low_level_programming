#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @t: is the number to be computed
 * Return: absolute value of the number
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
